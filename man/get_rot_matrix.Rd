% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry_utils.R
\name{get_rot_matrix}
\alias{get_rot_matrix}
\title{Compute a rotation matrix that maps vector \code{a} onto vector \code{b}}
\usage{
get_rot_matrix(a, b)
}
\arguments{
\item{a}{Numeric vector of length 3. The source direction (need not be
normalised; it will be normalised internally).}

\item{b}{Numeric vector of length 3. The target direction (need not be
normalised).}
}
\value{
A 3×3 numeric rotation matrix.
}
\description{
Uses the Rodrigues rotation formula to construct the 3×3 rotation matrix
that rotates unit vector \code{a} into unit vector \code{b}.
}
\details{
Two degenerate cases are handled:
\itemize{
\item \strong{Antiparallel} (\eqn{\cos\theta \approx -1}): a 180° rotation
about an arbitrary perpendicular axis is returned.
\item \strong{Parallel} (\eqn{\cos\theta \approx 1}): the identity matrix is
returned.
}
}
\examples{
# Rotate the Z-axis onto the X-axis
R <- get_rot_matrix(c(0, 0, 1), c(1, 0, 0))
round(R \%*\% c(0, 0, 1), 10)   # should equal c(1, 0, 0)

}
