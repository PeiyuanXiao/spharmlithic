% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz3d_utils.R
\name{add_scars_3d}
\alias{add_scars_3d}
\title{Add scar line segments and arrow cones to a Plotly 3-D figure}
\usage{
add_scars_3d(fig, sx, sy, sz, ex, ey, ez, highlight_idx = NULL)
}
\arguments{
\item{fig}{A \code{plotly} figure object.}

\item{sx, sy, sz}{Numeric vectors of scar start-point coordinates.}

\item{ex, ey, ez}{Numeric vectors of scar end-point coordinates.}

\item{highlight_idx}{Integer or \code{NULL}. Index of the scar to highlight.
When \code{NULL} (default) all scars are rendered uniformly in steel-blue.}
}
\value{
The modified \code{plotly} figure object.
}
\description{
Draws each scar as a line segment with a directional cone at its tip.
An optional index allows one scar to be rendered in a highlight style
(thicker, pink) — used in the Lin 2024 pipeline to emphasise the longest
scar.
}
\details{
Each scar is rendered as two Plotly traces: a \code{scatter3d} line for the
shaft and a \code{cone} trace for the arrowhead at the end-point.  The
highlighted scar (if any) uses a wider line (\code{width = 6} vs \code{2}) and a
larger cone (\code{sizeref = 0.08} vs \code{0.05}).
}
\examples{
\dontrun{
library(plotly)
fig <- plot_ly()
fig <- add_scars_3d(fig, sx = c(0, 1), sy = c(0, 0), sz = c(0, 0),
                         ex = c(1, 2), ey = c(0, 1), ez = c(0, 0))
fig

# Highlight the second scar (e.g. the longest)
fig2 <- plot_ly()
fig2 <- add_scars_3d(fig2, sx = c(0, 1), sy = c(0, 0), sz = c(0, 0),
                           ex = c(1, 2), ey = c(0, 1), ez = c(0, 0),
                           highlight_idx = 2)
fig2
}

}
\seealso{
\code{\link[=add_arrow_3d]{add_arrow_3d()}}, \code{\link[=add_plane_3d]{add_plane_3d()}}, \code{\link[=build_panel_scar]{build_panel_scar()}},
\code{\link[=build_panels_morph]{build_panels_morph()}}
}
