% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plane_angle.R
\name{compute_plane_angle}
\alias{compute_plane_angle}
\title{Compute angle between morphological plane and technical (SVD) plane}
\usage{
compute_plane_angle(df_group)
}
\arguments{
\item{df_group}{A data frame for a \strong{single} group. Required columns:
\describe{
\item{Norm_X, Norm_Y, Norm_Z}{Morphological plane normal (first row used).}
\item{Start_X, Start_Y, Start_Z}{Scar start-point coordinates.}
\item{End_X, End_Y, End_Z}{Scar end-point coordinates.}
}}
}
\value{
A one-row \code{data.frame} with column:
\describe{
\item{angle_deg}{Angle between the two plane normals (degrees), rounded
to 2 decimal places.}
}
Returns \code{NULL} if fewer than three valid scar vectors are present.
}
\description{
For a single group (e.g. one specimen or one stratigraphic layer), this
function calculates the angle (in degrees) between the morphological
best-fit plane (provided by Geomagic, stored as \code{Norm_X/Y/Z}) and the
technical best-fit plane derived from SVD of the scar direction vectors.
}
\examples{
\dontrun{
library(dplyr)
result <- my_data \%>\%
  group_by(ID) \%>\%
  group_modify(~ compute_plane_angle(.x))
}

}
\seealso{
\code{\link[=compute_scar_plane]{compute_scar_plane()}}
}
