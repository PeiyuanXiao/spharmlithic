% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_lin2024.R
\name{align_morph}
\alias{align_morph}
\title{Align scar orientation data using the Lin 2024 pipeline}
\usage{
align_morph(df_group)
}
\arguments{
\item{df_group}{A data frame for a \strong{single} specimen.  Must contain
columns \code{Start_X}, \code{Start_Y}, \code{Start_Z}, \code{End_X}, \code{End_Y}, \code{End_Z},
\code{Norm_X}, \code{Norm_Y}, \code{Norm_Z}, and optionally \code{Length}.}
}
\value{
The input data frame with nine additional columns:
\describe{
\item{s_x, s_y, s_z}{Aligned start-point coordinates.}
\item{e_x, e_y, e_z}{Aligned end-point coordinates.}
\item{d_x, d_y, d_z}{Aligned unit direction vectors.}
}
}
\description{
Implements the two-step alignment procedure described in Lin et al. (2024):
\enumerate{
\item \strong{Rotate} — uses the pre-measured morphological plane normal
(\code{Norm_X/Y/Z}) to rotate the scar data so that the normal points along
the Z-axis.
\item \strong{Translate} — shifts all coordinates so that the start-point of
the longest scar lies at the XY origin.
}
}
\details{
This is the \strong{morphology-driven} alignment method: the plane normal is
supplied externally (e.g. measured in Geomagic) rather than estimated from
the scar data.  This makes the rotation step independent of scar
configuration, which can be advantageous for cores with few or poorly
distributed scars.

For the data-driven alternative that estimates the plane normal from the
scars themselves, see \code{\link[=align_scar]{align_scar()}}.
}
\examples{
\dontrun{
# Single specimen
df_one <- subset(raw_data, ID == "S001")
aligned_one <- align_morph(df_one)

# All specimens via the batch wrapper
aligned_all <- align_morph_batch(raw_data)
}

}
\references{
Lin, S. C., Clarkson, C., Julianto, I. M. A., Ferdianto, A., & Sutikna,
T. (2024). A new method for quantifying flake scar organisation on cores
using orientation statistics. \emph{Journal of Archaeological Science},
\strong{167}, 105998.
}
\seealso{
\code{\link[=align_scar]{align_scar()}}, \code{\link[=get_scar_length]{get_scar_length()}}, \code{\link[=align_morph_batch]{align_morph_batch()}}
}
