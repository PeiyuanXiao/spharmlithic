% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{make_ilr}
\alias{make_ilr}
\title{Compute isometric log-ratio coordinates}
\usage{
make_ilr(x, delta = NULL, fraction = 0.65)
}
\arguments{
\item{x}{A numeric matrix or data frame of compositional data.}

\item{delta}{Optional replacement value passed to \code{\link[=replace_zeros]{replace_zeros()}}.}

\item{fraction}{Fraction of the smallest non-zero value used by
\code{\link[=replace_zeros]{replace_zeros()}} when \code{delta = NULL}.}
}
\value{
A data frame containing ILR coordinates.
}
\description{
Transform compositional data to isometric log-ratio (ILR) coordinates.
}
\details{
Columns with zero variance are removed before transformation because they
do not contribute information and can cause numerical problems.

Zeros are replaced using \code{\link[=replace_zeros]{replace_zeros()}} prior to transformation.
}
\examples{
x <- data.frame(
  a = c(0.5, 0.4, 0.6),
  b = c(0.3, 0.4, 0.2),
  c = c(0.2, 0.2, 0.2)
)

make_ilr(x)

}
