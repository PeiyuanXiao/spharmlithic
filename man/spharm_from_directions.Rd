% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spharm_directions.R
\name{spharm_from_directions}
\alias{spharm_from_directions}
\title{Spherical harmonic analysis from scar direction vectors}
\usage{
spharm_from_directions(
  data,
  lmax = 20,
  bandwidth = 0.35,
  n_bearing = 72,
  n_plunge = 36,
  dh_size = 64,
  id_col = "ID",
  dx_col = "d_x",
  dy_col = "d_y",
  dz_col = "d_z",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing direction vectors. Must contain an
ID column (default \code{"ID"}) and three direction-component columns
(default \code{"d_x"}, \code{"d_y"}, \code{"d_z"}, matching the output of
\code{align_scar_batch()} and \code{align_morph_batch()}). Rows where a direction vector
is zero or NA are silently dropped.}

\item{lmax}{Integer. Maximum spherical harmonic degree. Default 20.}

\item{bandwidth}{Numeric. vMF KDE bandwidth (smaller -> sharper peaks).
Default 0.35. This is an empirical value; users are encouraged to
conduct sensitivity analysis based on data characteristics
(specimen scar count, directional concentration).}

\item{n_bearing, n_plunge}{Integers. Resolution of the intermediate
evaluation grid for vMF KDE. Defaults 72 and 36 (5-degree spacing).}

\item{dh_size}{Integer. Driscoll-Healy grid latitude size; longitude is
\code{2 * dh_size}. Default 64.}

\item{id_col, dx_col, dy_col, dz_col}{Column names. Defaults match
\code{\link[=align_scar_batch]{align_scar_batch()}} output.}

\item{verbose}{Logical. Print per-specimen progress. Default \code{TRUE}.}
}
\value{
A list with one element per specimen, named by ID. Each
element is itself a list with:
\describe{
\item{coefficients}{Numeric array of shape \verb{(2, lmax+1, lmax+1)} -
spherical harmonic coefficients in pyshtools 4pi normalization.
Index \verb{[1, l, m]} is the cosine coefficient (m >= 0); index
\verb{[2, l, m]} is the sine coefficient (m > 0).}
\item{power_spectrum}{Numeric vector of length \code{lmax+1} - raw power
per degree.}
}
Use \code{\link[=spharm_to_dataframe]{spharm_to_dataframe()}} to flatten the list into a wide-format
data frame suitable for CSV export.
}
\description{
Pipeline (scar pattern analysis):
\enumerate{
\item Extract unit direction vectors \verb{(d_x, d_y, d_z)} from each scar.
\item Per-specimen von Mises-Fisher kernel density estimate on the sphere.
\item Interpolate KDE values onto a Driscoll-Healy regular grid.
\item Spherical harmonic expansion via \code{pyshtools} (4pi normalization).
}
}
\details{
This function calls the bundled Python backend via \code{reticulate}. Make
sure you have run \code{\link[=install_spharmlithic_python]{install_spharmlithic_python()}} (or
\code{\link[=use_spharmlithic_python]{use_spharmlithic_python()}}) at least once.
}
\examples{
\dontrun{
# Typical workflow
aligned <- align_scar_batch(my_scar_data)
result  <- spharm_from_directions(aligned, lmax = 20)

# Inspect one specimen
result[[1]]$power_spectrum

# Flatten for CSV / downstream analysis
df <- spharm_to_dataframe(result)
write.csv(df, "spharm_results.csv", row.names = FALSE)
}

}
\references{
Wieczorek, M. A., & Meschede, M. (2018). SHTools: Tools for working
with spherical harmonics. \emph{Geochemistry, Geophysics, Geosystems},
\strong{19}(8), 2574--2592.
}
\seealso{
\code{\link[=spharm_from_meshes]{spharm_from_meshes()}}, \code{\link[=spharm_to_dataframe]{spharm_to_dataframe()}}, \code{\link[=export_spharm_html]{export_spharm_html()}},
\code{\link[=install_spharmlithic_python]{install_spharmlithic_python()}}
}
