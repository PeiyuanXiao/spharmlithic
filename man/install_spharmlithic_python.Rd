% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_python.R
\name{install_spharmlithic_python}
\alias{install_spharmlithic_python}
\title{Install the Python backend for \code{spharmlithic}}
\usage{
install_spharmlithic_python(
  envname = "r-spharmlithic",
  method = c("auto", "conda", "virtualenv"),
  mesh = FALSE,
  python_version = ">=3.9,<3.13",
  new_env = identical(envname, "r-spharmlithic"),
  restart_session = TRUE
)
}
\arguments{
\item{envname}{Character. Conda environment name. Default
\code{"r-spharmlithic"}.}

\item{method}{Character. One of \code{"auto"}, \code{"conda"}, \code{"virtualenv"}.
Default \code{"auto"}, which prefers conda when available (recommended,
because \code{pyshtools} is easier to install via conda-forge).}

\item{mesh}{Logical. If \code{TRUE}, install the mesh-processing extension
(\code{trimesh}, \code{open3d}) needed for \code{\link[=spharm_from_meshes]{spharm_from_meshes()}}. Default
\code{FALSE} (core only).}

\item{python_version}{Character. Python version constraint passed to
reticulate. Default \code{">=3.9,<3.13"}.}

\item{new_env}{Logical. If \code{TRUE} (default when \code{envname} is the
package default), remove any existing environment with the same name
first.}

\item{restart_session}{Logical. Restart R session after install
(only affects RStudio).}
}
\value{
Invisibly, the environment name.
}
\description{
Creates (or recreates) a conda environment containing the Python
packages required for spherical harmonic analysis. By default installs
the \strong{core} environment, sufficient for the direction-vector pipeline
(\code{\link[=spharm_from_directions]{spharm_from_directions()}}). Pass \code{mesh = TRUE} to additionally
install \code{trimesh} and \code{open3d} for the STL pipeline
(\code{\link[=spharm_from_meshes]{spharm_from_meshes()}}).
}
\examples{
\dontrun{
# Core install (direction-vector pipeline only)
install_spharmlithic_python()

# Full install (also enables STL pipeline)
install_spharmlithic_python(mesh = TRUE)
}

}
\seealso{
\code{\link[=use_spharmlithic_python]{use_spharmlithic_python()}}
}
