% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spharm_utils.R
\name{spharm_reconstruct}
\alias{spharm_reconstruct}
\title{Inverse spherical harmonic transform: coefficients to spherical density}
\usage{
spharm_reconstruct(coefficients, grid_size = 64)
}
\arguments{
\item{coefficients}{Numeric or complex array of shape
\verb{(2, lmax+1, lmax+1)} — the \code{coefficients} element of one specimen's
\verb{spharm_from_*()} result. Both Track-A (complex) and Track-B (real)
coefficients are accepted.}

\item{grid_size}{Integer. Latitude resolution of the reconstruction
grid; longitude resolution is \code{2 * grid_size}. Default 64. Larger
values give smoother visualisations at higher cost.}
}
\value{
A list with:
\describe{
\item{density}{Numeric matrix of shape \verb{(grid_size, 2 * grid_size)}
- the reconstructed density on the Driscoll-Healy grid. Negative
values from finite-degree truncation are clipped to zero.}
\item{lon}{Numeric vector of length \code{2 * grid_size}. Longitude
(radians, range \verb{[0, 2*pi)}).}
\item{lat}{Numeric vector of length \code{grid_size}. Latitude (radians,
range \verb{(-pi/2, pi/2)}; equator = 0).}
\item{xyz}{Numeric matrix of shape \verb{(grid_size * 2 * grid_size, 3)}
- unit-sphere Cartesian coordinates for each grid cell, row-major
order matching \code{as.vector(density)}. Convenient input for
\code{plotly::plot_ly(type = "surface", surfacecolor = ...)}.}
}
}
\description{
\strong{Low-level utility.} For interactive 3D exploration of
reconstructed surfaces, \code{\link[=export_spharm_html]{export_spharm_html()}} is usually more
convenient. Use this function when you need the reconstructed density
as numerical data — for example, computing point-wise differences
between specimens, extracting density peaks, or building custom
visualisations with \code{plotly} or \code{ggplot2}.
}
\details{
Given a single specimen's spherical-harmonic coefficient array (as
returned in \verb{spharm_from_*()$<id>$coefficients}), reconstructs the
corresponding density on a Driscoll-Healy grid on the unit sphere.
The returned object also includes the longitude / latitude vectors and
an \verb{(n_lat * n_lon, 3)} matrix of unit-sphere Cartesian coordinates,
for direct plotting with \code{plotly::plot_ly()}.
}
\examples{
\dontrun{
result <- spharm_from_directions(my_aligned_data, lmax = 20)

# Reconstruct the spherical density for one specimen
rec <- spharm_reconstruct(result[[1]]$coefficients, grid_size = 64)

# Quick plotly visualisation
library(plotly)
nlat <- length(rec$lat); nlon <- length(rec$lon)
x_mat <- matrix(rec$xyz[, "x"], nlat, nlon)
y_mat <- matrix(rec$xyz[, "y"], nlat, nlon)
z_mat <- matrix(rec$xyz[, "z"], nlat, nlon)
plot_ly(x = x_mat, y = y_mat, z = z_mat,
        surfacecolor = rec$density,
        type = "surface", colorscale = "Hot",
        showscale = TRUE)

# Batch reconstruction with lapply()
all_recs <- lapply(result, function(s) spharm_reconstruct(s$coefficients))
}

}
\references{
Wieczorek, M. A., & Meschede, M. (2018). SHTools: Tools for working
with spherical harmonics. \emph{Geochemistry, Geophysics, Geosystems},
\strong{19}(8), 2574--2592.
}
\seealso{
\code{\link[=spharm_from_directions]{spharm_from_directions()}}, \code{\link[=spharm_from_meshes]{spharm_from_meshes()}}, \code{\link[=export_spharm_html]{export_spharm_html()}}
}
