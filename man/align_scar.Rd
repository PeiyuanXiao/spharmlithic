% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_svd.R
\name{align_scar}
\alias{align_scar}
\title{Align scar orientation data using SVD (three-step pipeline)}
\usage{
align_scar(df_group)
}
\arguments{
\item{df_group}{A data frame for a \strong{single} specimen.  Must contain
columns \code{Start_X}, \code{Start_Y}, \code{Start_Z}, \code{End_X}, \code{End_Y}, \code{End_Z}.}
}
\value{
The input data frame with nine additional columns:
\describe{
\item{s_x, s_y, s_z}{Aligned start-point coordinates.}
\item{e_x, e_y, e_z}{Aligned end-point coordinates.}
\item{d_x, d_y, d_z}{Aligned unit direction vectors.}
}
}
\description{
Performs the three-step SVD alignment pipeline on a single-specimen data
frame:
\enumerate{
\item \strong{Rotate} — SVD of the unit-direction matrix extracts the
best-fit plane normal; rotate it onto the Z-axis via
\code{\link[=get_rot_matrix]{get_rot_matrix()}}.
\item \strong{Translate} — shift the point-cloud centroid to the origin.
\item \strong{In-plane rotate} — SVD on the XY projections finds the main
scar direction and rotates it onto the X-axis.
}
}
\seealso{
\code{\link[=get_rot_matrix]{get_rot_matrix()}}, \code{\link[=align_morph]{align_morph()}}
}
