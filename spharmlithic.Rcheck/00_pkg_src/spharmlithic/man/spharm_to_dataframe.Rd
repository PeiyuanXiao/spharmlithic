% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spharm_utils.R
\name{spharm_to_dataframe}
\alias{spharm_to_dataframe}
\title{Flatten SPHARM result list into a wide-format data frame}
\usage{
spharm_to_dataframe(x, include_coeffs = TRUE)
}
\arguments{
\item{x}{A list returned by \code{\link[=spharm_from_directions]{spharm_from_directions()}} or
\code{\link[=spharm_from_meshes]{spharm_from_meshes()}}.}

\item{include_coeffs}{Logical. If \code{TRUE} (default), include all
flattened coefficients as columns named \code{coeff_0001}, \code{coeff_0002},
... If \code{FALSE}, return only the power spectrum (much smaller).}
}
\value{
A \code{tibble} with columns:
\describe{
\item{ID}{Specimen identifier.}
\item{power_l0, power_l1, ..., power_lN}{Raw power per spherical
harmonic degree (N = lmax).}
\item{coeff_0001, coeff_0002, ...}{Flattened real-valued coefficients
(only when \code{include_coeffs = TRUE}). For \code{\link[=spharm_from_meshes]{spharm_from_meshes()}},
which returns complex coefficients with negligible imaginary parts,
only the real part is retained.}
}
}
\description{
Converts the per-specimen list returned by \code{\link[=spharm_from_directions]{spharm_from_directions()}} or
\code{\link[=spharm_from_meshes]{spharm_from_meshes()}} into a single wide-format data frame, with one
row per specimen and one column per power-spectrum value and per
coefficient. Suitable for CSV export and downstream multivariate
analysis (PCA, UMAP, clustering) in R.
}
\examples{
\dontrun{
result <- spharm_from_directions(my_aligned_data, lmax = 20)

# Full output (power + coefficients)
df_full <- spharm_to_dataframe(result)

# Power spectrum only (smaller, easier to inspect)
df_power <- spharm_to_dataframe(result, include_coeffs = FALSE)

write.csv(df_full, "spharm_results.csv", row.names = FALSE)
}

}
\seealso{
\code{\link[=spharm_from_directions]{spharm_from_directions()}}, \code{\link[=spharm_from_meshes]{spharm_from_meshes()}}, \code{\link[=export_spharm_html]{export_spharm_html()}},
\code{\link[=spharm_reconstruct]{spharm_reconstruct()}}
}
