% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_python.R
\name{use_spharmlithic_python}
\alias{use_spharmlithic_python}
\title{Point \code{spharmlithic} at an existing Python environment}
\usage{
use_spharmlithic_python(envname, check_mesh = FALSE)
}
\arguments{
\item{envname}{Character. Name of an existing conda environment, OR a
path to a virtualenv root, OR a path to a Python executable.}

\item{check_mesh}{Logical. If \code{TRUE}, also verify mesh-extension
packages (\code{trimesh}, \code{open3d}). Default \code{FALSE}.}
}
\value{
Invisibly, a list with elements \code{envname}, \code{python} (path to
the active Python executable), and \code{available} (named logical
vector of required-package availability).
}
\description{
Use this if you already have a conda environment with the required
Python packages (e.g. an environment created by your analysis project's
\code{environment.yml}, or a Docker image). Performs a lightweight check
that the required packages are importable.
}
\details{
This function should be called \strong{before} any \code{\link[=spharm_from_directions]{spharm_from_directions()}}
or \code{\link[=spharm_from_meshes]{spharm_from_meshes()}} call, and ideally before the package's first
use in an R session.

The \code{envname} argument is resolved in the following order:
\enumerate{
\item \strong{Conda environment} — if \code{envname} matches a name returned
by \code{reticulate::conda_list()}, \code{reticulate::use_condaenv()} is
called.
\item \strong{Virtualenv} — if \code{reticulate::virtualenv_exists(envname)}
is \code{TRUE}, \code{reticulate::use_virtualenv()} is called.
\item \strong{Python executable path} — if \code{file.exists(envname)} is
\code{TRUE}, \code{reticulate::use_python()} is called with the path
directly.
}
If none of the above match, the function stops with an informative
error. In all cases \code{required = TRUE} is passed to the underlying
reticulate call, so any conflict with an already-initialised Python
session will raise an error rather than silently use a different
interpreter.
}
\examples{
\dontrun{
# Reuse the conda env created by your analysis project
use_spharmlithic_python("r-spharmlithic")

# Reuse and verify mesh extension is available
use_spharmlithic_python("r-spharmlithic", check_mesh = TRUE)
}

}
\seealso{
\code{\link[=install_spharmlithic_python]{install_spharmlithic_python()}}
}
