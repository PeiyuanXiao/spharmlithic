% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spharm_meshes.R
\name{spharm_from_meshes}
\alias{spharm_from_meshes}
\title{Spherical harmonic analysis from STL meshes}
\usage{
spharm_from_meshes(
  stl_dir,
  lmax = 20,
  target_faces = 20000,
  grid_size = 256,
  smooth_iterations = 3,
  pre_decimate_threshold = 3e+06,
  pre_decimate_target = 5e+05,
  verbose = TRUE
)
}
\arguments{
\item{stl_dir}{Character. Path to a directory containing \code{.stl} files.
Each STL is treated as one specimen; the file basename (without
extension) becomes its ID.}

\item{lmax}{Integer. Maximum spherical harmonic degree. Default 20.}

\item{target_faces}{Integer. Decimation target. Default 20000.}

\item{grid_size}{Integer. Latitude resolution of the interpolation
grid; the longitude direction uses \code{2 * grid_size} points
(Driscoll-Healy sampling 2). Default 256.}

\item{smooth_iterations}{Integer. Laplacian smoothing iterations after
decimation. Default 3. Set to 0 to skip smoothing.}

\item{pre_decimate_threshold}{Integer. Face count above which streaming
pre-decimation is triggered. Default 3000000.}

\item{pre_decimate_target}{Integer. Pre-decimation target face count.
Default 500000.}

\item{verbose}{Logical. Print per-file progress. Default \code{TRUE}.}
}
\value{
A list with one element per successfully-processed STL, named
by file basename. Each element is a list with \code{coefficients} and
\code{power_spectrum} (same structure as \code{\link[=spharm_from_directions]{spharm_from_directions()}}).
Failed specimens are reported via warnings and omitted from the
result.
}
\description{
Pipeline (morphological analysis), per STL file:
\enumerate{
\item Read mesh via \code{open3d}. Files exceeding 3M faces are
pre-decimated by streaming face sub-sampling to avoid memory pressure.
\item Quadric-error decimation to \code{target_faces}.
\item Laplacian smoothing (\code{smooth_iterations} passes).
\item Volume-centroid normalization to a unit sphere.
\item Area-weighted PCA alignment with energy-based sign convention.
\item Cartesian-to-spherical conversion of the aligned vertices.
\item Interpolation onto a regular \verb{(grid_size, grid_size)} lat-lon grid.
\item Spherical harmonic expansion via \code{pyshtools} (4pi normalization,
zero-component normalized: all coefficients divided by \code{c(0, 0)}).
}
}
\details{
Requires the \strong{mesh} Python extension. Run
\code{install_spharmlithic_python(mesh = TRUE)} once before first use.
}
\examples{
\dontrun{
# Requires mesh extension
install_spharmlithic_python(mesh = TRUE)

result <- spharm_from_meshes(
  stl_dir      = "data/3D_models",
  lmax         = 20,
  target_faces = 20000
)

df <- spharm_to_dataframe(result)
write.csv(df, "spharm_meshes.csv", row.names = FALSE)
}

}
\references{
Wieczorek, M. A., & Meschede, M. (2018). SHTools: Tools for working
with spherical harmonics. \emph{Geochemistry, Geophysics, Geosystems},
\strong{19}(8), 2574--2592.
}
\seealso{
\code{\link[=spharm_from_directions]{spharm_from_directions()}}, \code{\link[=spharm_to_dataframe]{spharm_to_dataframe()}}, \code{\link[=export_spharm_html]{export_spharm_html()}},
\code{\link[=install_spharmlithic_python]{install_spharmlithic_python()}}
}
